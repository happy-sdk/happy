project_name: happy-sdk/happy-go/strings/bexp

release:
  draft: false
  github:
    owner: happy-sdk
    name: happy-go

builds:
- skip: true

checksum:
  name_template: '{{ .ProjectName }}-{{ .Version }}-checksums.txt'

changelog:
  sort: desc
  filters:
    exclude:
    - '^.github:'
    - '^test:'
    - '^merge:'
    - '^devops:'
    - '^chore:'

monorepo:
  tag_prefix: strings/bexp/
  dir: strings/bexp
