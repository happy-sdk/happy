version: "1"

releaser:
  enabled: true
  bump:
    # A breaking change or a Go version sync bumps minor, jumped straight to
    # the next full hundred (e.g. v1.55.0 -> v1.100.0) instead of bumping
    # major - see the compatibility promise in README.md: stability is
    # guaranteed within a block of 100 minor versions, and crossing into the
    # next block is the signal that the minimum Go version or breaking
    # changes may follow.
    kind: minor
    strategy: hundred

linter:
  golangci-lint:
    # TEMPORARY: staticcheck's SA5012 analyzer panics on go1.27rc2 toolchains
    # ("interface conversion: interface {} is nil, not *buildir.IR") - an
    # upstream golangci-lint/honnef.co/go/tools incompatibility with the
    # go1.27 release candidate, not a real lint failure. Remove this once
    # golangci-lint ships a release built against a stable go1.27.
    disable:
      - staticcheck

deps:
  go:
    latest:
      - charm.land/bubbles/v2
      - charm.land/bubbletea/v2
      - charm.land/lipgloss/v2
      - github.com/fsnotify/fsnotify
      - codeberg.org/go-pdf/fpdf
      - github.com/go-git/go-git/v6

git:
  author:
    email: hello@happy-sdk.dev
    name: The Happy Authors
  branch: main
  remote:
    url: git@github.com:happy-sdk/happy.git

ignore:
  - pkg/logging/internal/bench
